// Re-export from @leafready/core for backwards compatibility within the web app.
export type { QuizStats } from "@leafready/core";
export { loadStats, saveStats, clearStats } from "@leafready/core";
